# Contributing

See the [README](README.md) for design token reference and deployment guide.

## Adding Tokens

1. Add to `tokens.json` (machine spec)
2. Add CSS custom property to `public/css/tokens.css`
3. Add swatch or demo to `public/index.html`
4. Update `README.md` token reference table

## Deployment

```bash
npm run deploy
```

